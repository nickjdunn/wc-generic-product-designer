<?php
/**
 * Optional wp-config.php snippets for WC Generic Product Designer.
 *
 * Enable server-side debug logging (overrides admin toggle when true):
 * define( 'WC_GPD_DEBUG', true );
 *
 * Enable frontend browser console debug (requires WC_GPD_DEBUG or admin toggle):
 * define( 'WC_GPD_JS_DEBUG', true );
 *
 * For private GitHub repos via Git Updater:
 * define( 'GITHUB_TOKEN', 'ghp_xxxxxxxxxxxxxxxxxxxx' );
 */
