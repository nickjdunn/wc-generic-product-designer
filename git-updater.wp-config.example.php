<?php
/**
 * Optional: add to wp-config.php on your store (above "That's all, stop editing!").
 * Replace the token with a GitHub fine-grained or classic PAT (repo read access only).
 *
 * define( 'GITHUB_TOKEN', 'ghp_xxxxxxxxxxxxxxxxxxxx' );
 */
