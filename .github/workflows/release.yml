name: Release plugin zip

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build installable zip
        run: |
          # Match GitHub "main" branch archive folder name so WordPress treats uploads as updates.
          PLUGIN_SLUG="wc-generic-product-designer-main"
          mkdir -p "build/${PLUGIN_SLUG}"
          rsync -a \
            --exclude='.git' \
            --exclude='.github' \
            --exclude='node_modules' \
            --exclude='build' \
            --exclude='.cursor' \
            --exclude='.idea' \
            --exclude='.vscode' \
            --exclude='*.log' \
            ./ "build/${PLUGIN_SLUG}/"
          cd build
          zip -r "../${PLUGIN_SLUG}.zip" "${PLUGIN_SLUG}"

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: wc-generic-product-designer-main.zip
          generate_release_notes: true
